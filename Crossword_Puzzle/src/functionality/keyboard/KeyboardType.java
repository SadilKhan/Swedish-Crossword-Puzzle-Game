package functionality.keyboard;

public enum KeyboardType {
	/*
	 * There are two type of keyoards. (1) Plain Keyboard - all the letters are enabled
	 * and (2) Random Keyboard - where only 4 random letters + one provided letter are enabled.
	 * The map method in utility.Util creates a map between type of squares and keyboards. 
	 * 
	 */
	PLAIN,RANDOM

}
